---
export const prerender = false;

const { slug } = Astro.params;

const links = {
  hostinger: "https://www.hostinger.com/?REFERRALCODE=DOVWORLDSYFT",
  bluehost: "...",
  interserver: "...",
  nordvpn: "...",
  cyberghost: "...",
  wordpress: "...",
  elementor: "...",
  "1password": "...",
  cloudways: "..."
};

const url = links[slug];

if (url && url !== "...") {
  console.log("Affiliate click:", slug);
  return Astro.redirect(url, 302);
}

return new Response("Link not available", { status: 404 });
---